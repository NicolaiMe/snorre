import styled from '@emotion/styled'

export const FooterLabelSpan = styled.span`
  color: white; //todo change to contrast color
  font-size: 14px;
  font-weight: bold;
  text-transform: uppercase;
  margin-bottom: 0.5em;
`
